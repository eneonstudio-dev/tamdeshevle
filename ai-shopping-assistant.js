(function(){
  "use strict";
  let root=null,messages=[],recognition=null,brainLoad=null,voiceActive=false,voiceFinal="",voiceInterim="",talkMode=false,silenceTimer=null,lastSuggestions=[],lastStrategies=[];
  const esc=v=>String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));
  const money=v=>Math.round(Number(v)||0).toLocaleString("ru-RU")+" ₽";
  const low=v=>String(v||"").toLowerCase().replace(/ё/g,"е");
  const storeName=id=>(typeof STORES!=="undefined"?STORES:[]).find(x=>x.id===id)?.name||id;
  function bay(state="idle"){const img=root?.querySelector("[data-ai-bai]");const map={idle:"bai-idle.webp",listening:"bai-peek.webp",thinking:"bai-peek.webp",warning:"bai-grumpy.webp",success:"bai-idle.webp"};if(img)img.src="assets/bai/"+(map[state]||map.idle);root?.setAttribute("data-bai-state",state)}
  function summary(){const s=TDShoppingState.get(),best=s.lastPlans?.[0];if(!best)return"";return `<section class="td-ai-summary"><div class="td-ai-summary-head"><b>Корзина · ${best.products.length}</b></div>${best.products.map(p=>`<div class="td-ai-line"><span>${esc(p.emoji)} ${esc(p.name)}<small>${esc(p.pack)} · ${esc(storeName(p.storeId))}</small></span><b>${money(p.price*p.quantity)}</b></div>`).join("")}<div class="td-ai-total"><span>Итого</span><strong>≈ ${money(best.total)}</strong></div></section>`}
  function suggestionHTML(){if(!lastSuggestions.length)return"";return `<div class="td-ai-suggestions">${lastSuggestions.map((s,i)=>`<button type="button" data-ai-suggestion="${i}">${esc(s)}</button>`).join("")}</div>`}
  function strategyHTML(){if(!lastStrategies.length)return"";const best=lastStrategies[0];return `<section class="td-ai-strategies"><div class="td-ai-strategies-head"><b>Варианты Бая</b><span>сравнил перед действием</span></div>${lastStrategies.map((s,i)=>`<button type="button" class="td-ai-strategy ${i===0?"recommended":""}" data-ai-strategy="${esc(s.id)}"><span class="td-ai-strategy-top"><b>${esc(s.title)}</b>${i===0?"<i>Я бы выбрал</i>":""}<strong>${money(s.total)}</strong></span><small>${esc(s.description)}</small><span class="td-ai-strategy-meta">${s.productCount} видов · ${s.unitCount} уп. · ${s.stores} магаз.</span></button>`).join("")}<small class="td-ai-strategies-note">Нажми вариант — только тогда я применю его к корзине.</small></section>`}
  function render(){if(!root)return;const s=root.querySelector(".td-ai-messages");s.innerHTML=messages.map(m=>`<div class="td-ai-msg ${m.role}">${esc(m.text)}</div>`).join("")+strategyHTML()+suggestionHTML()+summary();s.querySelectorAll("[data-ai-suggestion]").forEach(b=>b.onclick=()=>submit(lastSuggestions[Number(b.dataset.aiSuggestion)]));s.querySelectorAll("[data-ai-strategy]").forEach(b=>b.onclick=()=>applyStrategy(b.dataset.aiStrategy));s.lastElementChild?.scrollIntoView({block:"nearest"});const tm=root.querySelector("[data-ai-talkmode]");if(tm){tm.classList.toggle("on",talkMode);tm.textContent=talkMode?"Диалог · вкл":"Диалог"}}
  function speak(text){return new Promise(resolve=>{if(!("speechSynthesis"in window)||!text){resolve();return}window.speechSynthesis.cancel();const u=new SpeechSynthesisUtterance(text);u.lang="ru-RU";u.rate=1.02;u.onend=()=>resolve();u.onerror=()=>resolve();window.speechSynthesis.speak(u)})}
  async function ensureBrain(){if(!window.TDBaiBrain){brainLoad=brainLoad||import("./bai-brain.js?v=20260911-native-v2").catch(e=>{console.warn("[Bai Brain] load failed",e);return null});await brainLoad}return window.TDBaiBrain||null}
  function plannerRequested(text,routed,result){const s=TDShoppingState.get(),t=low(text);if(!window.TDBaiPlanner||!s.budget||result?.needsClarification)return false;if(/вариант|что лучше|что бы ты выбрал|сам реши|предложи|не знаю что хочу|сравни/.test(t))return true;if(routed?.expectsAnswer)return false;if(/собери|подбери|на вечер|на ужин|нужна еда|хочу еды/.test(t))return true;const ops=routed?.operations||[];return ops.some(o=>["ADD_PREFERENCE","SET_COOKING","CHANGE_BUDGET","SET_PEOPLE","SET_DURATION"].includes(o.type))&&!ops.some(o=>["ADD_PRODUCT","REMOVE_PRODUCT","REPLACE_PRODUCT","SET_ONLY_PRODUCTS"].includes(o.type));}
  function planAlternatives(text){const s=TDShoppingState.get(),pack=window.TDBaiPlanner?.build?.(s,text);if(!pack?.strategies?.length)return"";lastStrategies=pack.strategies.slice(0,3);return window.TDBaiPlanner.explain(pack,s)}
  async function applyStrategy(id){const chosen=lastStrategies.find(x=>x.id===id);if(!chosen)return;clearTimeout(silenceTimer);if(voiceActive)stopVoice(false);messages.push({role:"user",text:`Выбираю «${chosen.title}»`});lastSuggestions=[];lastStrategies=[];bay("thinking");render();const result=TDShoppingConversation.apply(chosen.actionText,chosen.operations);const reply=`Беру «${chosen.title}». ${chosen.description}. ${result.message||"Корзину пересчитал."}`;messages.push({role:"assistant",text:reply});bay("success");render();await speak(reply);if(talkMode&&root)setTimeout(()=>startVoice(true),250)}
  async function submit(text){
    clearTimeout(silenceTimer);silenceTimer=null;
    if(voiceActive)stopVoice(false);
    const area=root?.querySelector("textarea"),value=String(text||area?.value||"").trim();if(!value)return;
    const history=messages.slice(-12);messages.push({role:"user",text:value});lastSuggestions=[];lastStrategies=[];if(area)area.value="";bay("thinking");render();
    let routed={operations:null,reply:"",suggestions:[]};try{const brain=await ensureBrain();routed=await brain?.route?.(value,history)||routed}catch(e){console.warn("[Bai Brain]",e)}
    let result=null,reply="";
    if(Array.isArray(routed.operations)&&routed.operations.length){result=TDShoppingConversation.apply(value,routed.operations);reply=result.message||routed.reply;}
    else if(routed.reply){reply=routed.reply;}
    else{result=TDShoppingConversation.apply(value);reply=result.message;}
    if(routed.reply&&!result?.needsClarification&&result?.message&&result.message!==routed.reply)reply=routed.reply+` ${result.message}`;
    lastSuggestions=Array.isArray(routed.suggestions)?routed.suggestions.slice(0,4):[];
    if(plannerRequested(value,routed,result)){
      const explanation=planAlternatives(value);
      if(explanation){reply=`${explanation} Я ничего не переключаю сам — покажу варианты, а применю тот, который ты выберешь.`;lastSuggestions=[];}
    }
    messages.push({role:"assistant",text:reply});bay(result?.needsClarification?"warning":"success");render();await speak(reply);
    if(talkMode&&root){setTimeout(()=>startVoice(true),250)}
  }
  function dedupe(t){let w=String(t||"").trim().split(/\s+/).filter(Boolean);let changed=true;while(changed){changed=false;for(let n=Math.min(14,Math.floor(w.length/2));n>0;n--){const a=w.slice(-n).join(" ").toLowerCase(),b=w.slice(-2*n,-n).join(" ").toLowerCase();if(a===b){w.splice(-n);changed=true;break}}}return w.filter((x,i)=>!i||x.toLowerCase()!==w[i-1].toLowerCase()).join(" ")}
  function updateVoice(){const a=root?.querySelector("textarea");if(a)a.value=dedupe([voiceFinal,voiceInterim].filter(Boolean).join(" "))}
  function armAutoSend(){if(!talkMode)return;clearTimeout(silenceTimer);silenceTimer=setTimeout(()=>{const txt=dedupe(voiceFinal);if(txt&&voiceActive)submit(txt)},1300)}
  function makeRecognition(){const SR=window.SpeechRecognition||window.webkitSpeechRecognition;if(!SR)return null;const r=new SR();r.lang="ru-RU";r.interimResults=true;r.continuous=true;r.maxAlternatives=1;r.onresult=e=>{let interim="";let gotFinal=false;for(let i=e.resultIndex;i<e.results.length;i++){const p=e.results[i][0]?.transcript||"";if(e.results[i].isFinal){voiceFinal=dedupe(voiceFinal+" "+p);gotFinal=true}else interim+=" "+p}voiceInterim=dedupe(interim);updateVoice();if(gotFinal)armAutoSend()};r.onend=()=>{recognition=null;if(voiceActive&&!silenceTimer)setTimeout(()=>{recognition=makeRecognition();try{recognition?.start()}catch{}},180)};return r}
  function startVoice(fromTalkMode=false){if(voiceActive)return;window.TDBaiVoice?.stop?.();voiceFinal="";voiceInterim="";voiceActive=true;recognition=makeRecognition();if(!recognition){voiceActive=false;messages.push({role:"assistant",text:"На этом устройстве голосовой ввод не поддерживается — напиши текстом."});render();return}try{recognition.start()}catch{}root?.querySelector("[data-ai-mic]")?.classList.add("listening");bay("listening");if(fromTalkMode)root?.setAttribute("data-talk-listening","1")}
  function stopVoice(clearTalkAttr=true){voiceActive=false;voiceInterim="";clearTimeout(silenceTimer);silenceTimer=null;try{recognition?.stop()}catch{}recognition=null;root?.querySelector("[data-ai-mic]")?.classList.remove("listening");if(clearTalkAttr)root?.removeAttribute("data-talk-listening");updateVoice();bay("idle")}
  function voice(){if(voiceActive){stopVoice();return}startVoice()}
  function toggleTalkMode(){talkMode=!talkMode;if(talkMode){startVoice(true)}else{stopVoice();window.speechSynthesis?.cancel?.()}render()}
  function newSession(){stopVoice();talkMode=false;TDShoppingState.reset();window.TDBaiBrain?.reset?.();lastSuggestions=[];lastStrategies=[];messages=[{role:"assistant",text:"Начали заново. Опиши задачу как человеку — я сам уточню, чего не хватает, и перед действием сравню нормальные варианты."}];render()}
  function open(){if(root)return;root=document.createElement("section");root.className="td-ai";root.innerHTML=`<div class="td-ai-shell"><header class="td-ai-head"><button data-ai-close>←</button><div><b>Бай</b><small>AI-помощник покупок</small></div><button class="td-ai-talkmode" data-ai-talkmode>Диалог</button><button data-ai-new>Новая</button></header><main class="td-ai-main"><div class="td-ai-bai"><img data-ai-bai src="assets/bai/bai-idle.webp" alt=""><h2>Что сегодня покупаем?</h2><p>Можешь говорить расплывчато. Я уточню, сравню варианты и скажу, какой выбрал бы сам.</p></div><div class="td-ai-messages" aria-live="polite"></div></main><div class="td-ai-compose"><textarea placeholder="Например: хочу что-нибудь на вечер, но сам не знаю что"></textarea><button data-ai-mic aria-label="Говорить">●</button><button class="td-ai-send" data-ai-send>Отправить</button></div></div>`;document.body.appendChild(root);root.querySelector("[data-ai-close]").onclick=()=>{talkMode=false;stopVoice();window.speechSynthesis?.cancel?.();root.remove();root=null};root.querySelector("[data-ai-new]").onclick=newSession;root.querySelector("[data-ai-talkmode]").onclick=toggleTalkMode;root.querySelector("[data-ai-send]").onclick=()=>submit();root.querySelector("[data-ai-mic]").onclick=voice;root.querySelector("textarea").onkeydown=e=>{if(e.key==="Enter"&&!e.shiftKey){e.preventDefault();submit()}};messages.length||(messages=[{role:"assistant",text:"Расскажи задачу как есть. Если данных мало, я сам спрошу. Если решений несколько — сравню их по цене и смыслу, а не ткну первое попавшееся."}]);render();ensureBrain();root.querySelector("textarea").focus()}
  function mutateProduct(type,id,fn){TDShoppingState.commit(type,s=>{const p=s.products.find(x=>x.id===id);if(p)fn(s,p)},type);TDShoppingState.syncCart();render()}
  function adjust(id,d){mutateProduct("CHANGE_QUANTITY",id,(s,p)=>{p.quantity=Math.max(0,p.quantity+d);if(!p.quantity)s.products=s.products.filter(x=>x.id!==id)})}
  function remove(id){mutateProduct("REMOVE_PRODUCT",id,s=>{s.products=s.products.filter(x=>x.id!==id);if(!s.excludedProducts.includes(id))s.excludedProducts.push(id)})}
  window.TDShoppingAssistant={open,voice,submit,adjust,remove,newSession,toggleTalkMode,applyStrategy};
})();
