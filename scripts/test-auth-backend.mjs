import fs from 'node:fs';
import assert from 'node:assert/strict';
const auth=fs.readFileSync(new URL('../auth-client.js',import.meta.url),'utf8');
const ui=fs.readFileSync(new URL('../account-auth-ui.js',import.meta.url),'utf8');
const html=fs.readFileSync(new URL('../index.html',import.meta.url),'utf8');
const sql=fs.readFileSync(new URL('../supabase/schema.sql',import.meta.url),'utf8');
assert.match(auth,/signInWithOtp/);
assert.match(auth,/persistSession:true/);
assert.match(auth,/syncLocalToCloud/);
assert.match(auth,/basket_history/);
assert.match(auth,/SUPABASE_NOT_CONFIGURED/);
assert.match(ui,/signOut/);
assert.match(html,/supabase-config\.js/);
assert.match(html,/auth-client\.js/);
assert.match(sql,/enable row level security/i);
assert.match(sql,/auth\.uid\(\) = user_id/);
assert.match(sql,/create table if not exists public\.baskets/);
assert.match(sql,/create table if not exists public\.addresses/);
assert.match(sql,/create table if not exists public\.basket_history/);
console.log('Auth backend checks passed: magic-link auth, session persistence, sync contract and RLS schema are wired.');
