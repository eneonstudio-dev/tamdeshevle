(()=>{"use strict";
const KEY="td_bai_learning_v1",MAX_CASES=80,MAX_TEXT=220,ALLOWED=new Set(["ADD_PRODUCT","REMOVE_PRODUCT","REPLACE_PRODUCT","REQUIRE","SET_ONLY_PRODUCTS","SET_PRODUCT_AMOUNT","CHANGE_BUDGET","SET_PEOPLE","SET_DURATION","SET_COOKING","ADD_PREFERENCE","PREFER","CHANGE_STORE","SET_MODE","RESET_BASKET","CLEAR_ONLY"]);
const low=v=>String(v||"").toLowerCase().replace(/ё/g,"е").replace(/\s+/g," ").trim();
const fallbackRedact=v=>String(v||"").replace(/[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}/gi,"[email]").replace(/(?:\+?\d[\d\s()\-]{8,}\d)/g,"[phone]").replace(/https?:\/\/\S+/gi,"[url]").slice(0,MAX_TEXT);
const redact=v=>window.TDBaiLearningSafety?.redact?.(v)||fallbackRedact(v);
const correction=t=>/(^|[\s,.;!?])(нет|не так|я имел|я хотел|имел в виду|хотел сказать|исправь|поправь|лучше|вместо|не это|не то)(?=$|[\s,.;!?])/.test(low(t));
const tokens=t=>new Set(low(t).replace(/[^а-яa-z0-9 ]/g," ").split(/\s+/).filter(x=>x.length>1));
const similarity=(a,b)=>{const A=tokens(a),B=tokens(b);if(!A.size||!B.size)return 0;let hit=0;for(const x of A)if(B.has(x))hit++;return hit/Math.max(A.size,B.size)};
const clone=v=>JSON.parse(JSON.stringify(v));
const safeOps=ops=>window.TDBaiLearningSafety?.safeOps?.(ops)||(Array.isArray(ops)?ops:[]).filter(o=>o&&ALLOWED.has(o.type)).slice(0,12).map(clone);
const opsKey=ops=>JSON.stringify(safeOps(ops));
function blank(){return{version:3,cases:[],observations:0,corrections:0,applied:0,quarantined:0,rejected:0,remoteSubmitted:0,remoteRejected:0}}
let db;try{db={...blank(),...JSON.parse(localStorage.getItem(KEY)||"{}")}}catch{db=blank()}db.cases=Array.isArray(db.cases)?db.cases:[];db.version=3;
let last=null,wrappedBrain=false,wrappedMemory=false;
function save(){db.cases=db.cases.slice(-MAX_CASES);try{localStorage.setItem(KEY,JSON.stringify(db))}catch{}return db}
function gate(input,fix,operations){return window.TDBaiLearningSafety?.evaluate?.({input,correction:fix,operations})||null}
async function submitRemote(item){const cfg=window.TD_BAI_LEARNING;if(!cfg?.endpoint||!window.TDAuth?.init)return null;const auth=await window.TDAuth.init();if(!auth||!window.TDAuth.user?.())return null;const {data,error}=await auth.auth.getSession();const token=data?.session?.access_token;if(error||!token)return null;const payload={fingerprint:item.safety?.fingerprint||"",input:item.input,correction:item.correction,operations:clone(item.operations),localConfirmations:item.count||1,clientTrustScore:item.safety?.score||0,requiresServerConsensus:true};const response=await fetch(cfg.endpoint,{method:"POST",headers:{"Content-Type":"application/json",Authorization:`Bearer ${token}`},body:JSON.stringify(payload)});let body=null;try{body=await response.json()}catch{}if(!response.ok&&response.status!==202)throw new Error(`BAI_LEARNING_REMOTE_${response.status}`);if(body?.accepted===false)db.remoteRejected++;else if(body?.accepted===true)db.remoteSubmitted++;save();try{window.dispatchEvent(new CustomEvent("td:bai-learning-remote",{detail:body||{status:response.status}}))}catch{}return body}
function addCase(input,correctionText,operations,verdict){const v=verdict||gate(input,correctionText,operations);if(!v?.personalAllowed)return null;const original=v.sanitized.input,fix=v.sanitized.correction,ops=v.sanitized.operations;if(!original||!ops.length)return null;const n=low(original),k=opsKey(ops);let item=db.cases.find(x=>x.normalized===n&&opsKey(x.operations)===k),created=false;const safety={score:v.score,fingerprint:v.fingerprint,globalCandidateEligible:v.globalCandidateEligible,requiresServerConsensus:true};if(item){item.count=(item.count||1)+1;item.lastSeen=Date.now();item.correction=fix||item.correction;item.safety=safety}else{created=true;item={input:original,normalized:n,correction:fix,operations:ops,count:1,createdAt:Date.now(),lastSeen:Date.now(),safety};db.cases.push(item)}db.corrections++;window.TDBaiLearningSafety?.markAccepted?.();save();if(created&&safety.globalCandidateEligible)submitRemote(clone(item)).catch(()=>{});return clone(item)}
function observe(raw,result){const rawText=String(raw||""),text=redact(rawText),ops=safeOps(result?.operations);db.observations++;if(last&&correction(text)&&ops.length){const previousRaw=last.raw||last.text,v=gate(previousRaw,rawText,ops);if(v?.personalAllowed)addCase(previousRaw,rawText,ops,v);else{if(v?.hardBlock)db.rejected++;else db.quarantined++;window.TDBaiLearningSafety?.quarantine?.({input:previousRaw,correction:rawText,operations:ops},v||undefined)}}last={raw:rawText,text,operations:ops,at:Date.now()};save()}
function best(text){const n=low(redact(text)),safety=window.TDBaiLearningSafety;if(!n||!safety)return null;let winner=null,score=0;for(const item of db.cases){const v=safety.evaluate({input:item.input,correction:item.correction,operations:item.operations});if(!v.personalAllowed)continue;let s=item.normalized===n?1:similarity(item.input,n);if(s<.96)continue;s+=Math.min(.02,Math.max(0,(item.count||1)-1)*.005);if(s>score){score=s;winner=item}}return winner?{case:clone(winner),confidence:Math.min(1,score)}:null}
function sameOps(a,b){return opsKey(a)===opsKey(b)}
function wrapBrain(){if(wrappedBrain||!window.TDBaiBrain?.route)return;wrappedBrain=true;const original=window.TDBaiBrain.route.bind(window.TDBaiBrain);window.TDBaiBrain.route=async function(raw,...rest){const learned=best(raw),routed=await original(raw,...rest);if(!learned)return routed;if(routed?.expectsAnswer||routed?.selfCheck?.safe===false)return routed;const learnedOps=safeOps(learned.case.operations);if(!learnedOps.length)return routed;if(sameOps(routed?.operations,learnedOps))return{...routed,learning:{matched:true,confidence:learned.confidence,applied:false}};db.applied++;save();return{...routed,provider:"bai-learning-loop",operations:learnedOps,reply:routed?.reply||"Понял по твоему прошлому исправлению.",learning:{matched:true,confidence:learned.confidence,applied:true}}}}
function wrapMemory(){if(wrappedMemory||!window.TDBaiMemory?.learn)return;wrappedMemory=true;const original=window.TDBaiMemory.learn.bind(window.TDBaiMemory);window.TDBaiMemory.learn=function(raw,result){const out=original(raw,result);try{observe(raw,result);wrapBrain()}catch{}return out}}
function clear(){db=blank();last=null;try{localStorage.removeItem(KEY)}catch{}return clone(db)}
function stats(){return{version:db.version,cases:db.cases.length,observations:db.observations,corrections:db.corrections,applied:db.applied,quarantined:db.quarantined,rejected:db.rejected,remoteSubmitted:db.remoteSubmitted||0,remoteRejected:db.remoteRejected||0,safety:window.TDBaiLearningSafety?.stats?.()||null}}
function exportCandidates(){return db.cases.filter(x=>x.safety?.globalCandidateEligible).map(x=>({fingerprint:x.safety.fingerprint,input:x.input,correction:x.correction,operations:clone(x.operations),localConfirmations:x.count||1,clientTrustScore:x.safety.score,requiresServerConsensus:true}))}
window.TDBaiLearning={observe,best,stats,clear,exportCases:()=>clone(db.cases),exportCandidates,submitRemoteCandidate:submitRemote};
wrapMemory();wrapBrain();
})();
