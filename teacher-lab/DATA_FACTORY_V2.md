# Bai Data Factory v2

Build review-only shopping training candidates with the existing pinned Qwen3-8B teacher profile. Generated model outputs remain candidates until explicit review and the existing provenance firewall accepts them. The frozen promotion holdout is never used as training input.
