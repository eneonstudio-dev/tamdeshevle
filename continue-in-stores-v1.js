(()=>{
  "use strict";
  const KEY="td:continue-stores-progress:v1";
  const esc=s=>String(s??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));
  const state=()=>window.TDShoppingState?.get?.()||{};
  const plan=()=>state().lastPlans?.[0]||null;
  const retailerModule=()=>window.TDRealStoreIntegrationV1?Promise.resolve(window.TDRealStoreIntegrationV1):import("./real-store-integration-v1.js?v=20260912-store-progress-v1").then(()=>window.TDRealStoreIntegrationV1);
  function counts(){const p=plan();const map=new Map();for(const item of p?.products||[]){const id=String(item.storeId||"").split("_")[0];map.set(id,(map.get(id)||0)+1)}return map}
  function signature(){const p=plan();return (p?.products||[]).map(x=>`${String(x.storeId||"").split("_")[0]}:${x.id||x.productId||x.name||"?"}:${x.quantity||1}`).sort().join("|")}
  function read(){try{const v=JSON.parse(localStorage.getItem(KEY)||"null");return v&&v.signature===signature()&&v.stores?v:{signature:signature(),stores:{}}}catch{return{signature:signature(),stores:{}}}}
  function write(v){try{localStorage.setItem(KEY,JSON.stringify(v))}catch{}}
  function progress(id,total){const v=read(),done=Math.max(0,Math.min(total,Number(v.stores[id])||0));return{done,total}}
  function setProgress(id,done,total){const v=read();v.stores[id]=Math.max(0,Math.min(total,Number(done)||0));write(v);renderProgress(id);return progress(id,total)}
  function renderProgress(id){const root=document.querySelector(`.td-continue-stores [data-store-id="${CSS.escape(id)}"]`);if(!root)return;const total=Number(root.dataset.total)||0,p=progress(id,total),label=root.querySelector("[data-store-progress]");if(label)label.textContent=`${p.done}/${p.total} готово`;root.classList.toggle("is-done",p.total>0&&p.done===p.total)}
  function close(){document.querySelector(".td-continue-stores")?.remove()}
  async function open(){const mod=await retailerModule();const retailers=mod?.supportedRetailers?.()||[],c=counts(),active=retailers.filter(r=>(c.get(r.id)||0)>0);if(!active.length)return false;close();const total=[...c.values()].reduce((a,b)=>a+b,0),modal=document.createElement("div");modal.className="td-continue-stores";modal.innerHTML=`<section class="td-continue-stores-card" role="dialog" aria-modal="true"><button class="td-continue-stores-x" type="button" aria-label="Закрыть">×</button><small>Корзина готова</small><h2>Продолжить в магазинах</h2><p>Отмечай прогресс вручную — он сохранится на этом устройстве, и можно будет вернуться позже.</p><div class="td-continue-stores-summary"><b>${total}</b><span>${active.length>1?`${active.length} магазина`:`${active.length} магазин`}</span></div><div class="td-continue-stores-list">${active.map((r,i)=>{const n=c.get(r.id)||0,p=progress(r.id,n);return`<div class="td-continue-store-row" data-store-id="${esc(r.id)}" data-total="${n}"><button type="button" data-open-store="${esc(r.id)}"><span><small>Шаг ${i+1}</small><b>${esc(r.name)}</b><em>${n} поз. · <i data-store-progress>${p.done}/${p.total} готово</i></em></span><strong>Открыть →</strong></button><div class="td-store-progress-controls"><button type="button" data-minus="${esc(r.id)}" aria-label="Уменьшить прогресс">−</button><button type="button" data-plus="${esc(r.id)}">Добавил товар</button></div></div>`}).join("")}</div><p class="td-continue-stores-note">«Проще» не читает корзину магазина. Прогресс — твоя отметка, а цена, наличие и фактическое добавление подтверждаются самим магазином.</p></section>`;document.body.appendChild(modal);modal.onclick=e=>{if(e.target===modal)close()};modal.querySelector(".td-continue-stores-x").onclick=close;modal.querySelectorAll("[data-open-store]").forEach(btn=>btn.onclick=()=>mod.open?.(btn.dataset.openStore));modal.querySelectorAll("[data-plus]").forEach(btn=>btn.onclick=()=>{const id=btn.dataset.plus,row=btn.closest("[data-store-id]"),n=Number(row.dataset.total)||0,p=progress(id,n);setProgress(id,p.done+1,n)});modal.querySelectorAll("[data-minus]").forEach(btn=>btn.onclick=()=>{const id=btn.dataset.minus,row=btn.closest("[data-store-id]"),n=Number(row.dataset.total)||0,p=progress(id,n);setProgress(id,p.done-1,n)});active.forEach(r=>renderProgress(r.id));return true}
  function css(){if(document.querySelector("style[data-continue-stores-v1]"))return;const s=document.createElement("style");s.dataset.continueStoresV1="1";s.textContent=`.td-continue-stores{position:fixed;inset:0;z-index:10080;background:rgba(8,18,13,.58);display:grid;place-items:end center;padding:16px}.td-continue-stores-card{position:relative;width:min(560px,100%);max-height:88vh;overflow:auto;background:#fff;border-radius:26px;padding:22px}.td-continue-stores-x{position:absolute;right:16px;top:14px;border:0;background:#eef4f0;width:36px;height:36px;border-radius:50%;font-size:24px}.td-continue-stores-card>small{color:#16834d;font-weight:900}.td-continue-stores-card h2{margin:5px 42px 6px 0;font-size:27px}.td-continue-stores-card p{color:#65736b}.td-continue-stores-summary{display:flex;gap:8px;align-items:baseline;background:#f2f8f4;border-radius:16px;padding:12px 14px;margin:14px 0}.td-continue-stores-summary b{font-size:24px}.td-continue-stores-list{display:grid;gap:9px}.td-continue-store-row{border:1px solid #e2ebe5;border-radius:17px;overflow:hidden}.td-continue-store-row>button{width:100%;border:0;background:#fff;padding:14px;display:flex;align-items:center;justify-content:space-between;gap:12px;text-align:left;cursor:pointer}.td-continue-store-row span{display:grid;gap:2px}.td-continue-store-row small,.td-continue-store-row em{color:#65736b;font-style:normal}.td-continue-store-row i{font-style:normal;font-weight:800;color:#0d7b4a}.td-continue-store-row strong{color:#0d7b4a}.td-continue-store-row.is-done{border-color:#20c979;background:#f1fff7}.td-store-progress-controls{display:flex;border-top:1px solid #edf2ef}.td-store-progress-controls button{border:0;background:#f8fbf9;padding:10px 12px;font-weight:800;cursor:pointer}.td-store-progress-controls button:first-child{width:48px;border-right:1px solid #edf2ef}.td-store-progress-controls button:last-child{flex:1;color:#0d7b4a}.td-continue-stores-note{font-size:12px}@media(max-width:520px){.td-continue-stores{padding:0}.td-continue-stores-card{border-radius:24px 24px 0 0;padding:20px 16px}}`;document.head.appendChild(s)}
  css();window.TDContinueInStoresV1={open,close,counts,progress,setProgress,signature};
})();